@model IEnumerable<Inmbiliariagestion.Models.Inmobiliaria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Telefon)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Telefon)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RazonSocial }) |
            @Html.ActionLink("Details", "Details", new { id=item.RazonSocial }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RazonSocial })
        </td>
    </tr>
}

</table>
